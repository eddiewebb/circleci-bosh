#!/bin/bash

#
# This is required because nginx tries to listen on ipv6 address, but bosh stemcells are hardended to remove ipv6 support
#

#set -eux

#rm /etc/sysctl.d/60-bosh-sysctl*
#sysctl --system
